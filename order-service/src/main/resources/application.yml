server:
    port: 8081
spring:
    datasource:
        password:
        url: jdbc:h2:mem:testdb
        username: sa
    h2:
        console:
          enabled: true
    jpa:
        show-sql: true
    application:
         name: ORDER-SERVICE

